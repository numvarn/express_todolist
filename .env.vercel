# Environment Variables Template สำหรับ Vercel Production
# คัดลอกค่าเหล่านี้ไปใส่ใน Vercel Dashboard > Settings > Environment Variables

# MongoDB Atlas Connection String (REQUIRED)
# แทนที่ด้วยค่าจริงจากไฟล์ .env ของคุณ
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority

# Node Environment
NODE_ENV=production

# หมายเหตุ: 
# - PORT จะถูกจัดการโดย Vercel อัตโนมัติ ไม่ต้องตั้งค่า
# - อย่าเปิดเผยไฟล์นี้ใน repository